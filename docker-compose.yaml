services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env          # TELOXIDE_TOKEN=...
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./data:/app/data
